<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up</title>
    <!-- <link rel="stylesheet" href="../used.css"> -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-aFq/bzH65dt+w6FI2ooMVUpc+21e0SRygnTpmBvdBgSdnuTN7QbdgL+OapgHtvPp" crossorigin="anonymous">
</head>
<style>
    /* .form>label{
        padding-left: 2rem;
    } */
    .form{
        padding: 2rem;
    }
</style>
<body>
    <div class="form w-50 d-block mx-auto">
        <h2 class="text-success text-center ">Budget Application</h2>
        <div class="d-block mx-auto">
            <% if (message!="") { %>
                <div id="messages" class="text-center border border-2 border-danger alert alert-danger"><%= message %></div>
            <% } %>
        </div>
        <form action="/" method="post" class="w-50 d-block mx-auto">            
            <label for="budgetName">Budget Name</label>
            <input type="text" class="form-control my-2" placeholder="Budget Name" name="budgetName">
            <label for="price">Price</label>
            <input type="text" class="form-control my-2" placeholder="Password" name="price">
            <label for="date">Date</label>
            <input type="datetime-local" class="form-control my-2" placeholder="" name="date">
            <button class="w-100 my-2 btn btn-success" type="submit">Add Budget</button>
        </form>
        <!-- <%= message %> -->
    </div>

    <div class="w-100 d-flex justify-content-center align-item-center">
        <% if (budgets != "" || budgets != []) { %>
            <table>
                <thead>
                    <tr>
                        <td class="text-center border border-2 border-danger">S/N</td>
                        <td class="text-center border border-2 border-danger">Budget Name</td>
                        <td class="text-center border border-2 border-danger">Budget Price</td>
                        <td class="text-center border border-2 border-danger">Budget Date</td>
                    </tr>
                </thead>
                <tbody>
                    <% for( let i = 0; i < budgets.length; i++ ) { %>
                        <tr>
                            <td class="text-center border border-2 border-danger"><%= i+1 %></td>
                            <td class="text-center border border-2 border-danger"><%= budgets[i].budgetName %></td>
                            <td class="text-center border border-2 border-danger"><%= budgets[i].price %></td>
                            <td class="text-center border border-2 border-danger"><%= budgets[i].date %></td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        <% } %>
    </div>
</body>
</html>